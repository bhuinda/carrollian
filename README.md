# d20

`d20` is the pending alias of the finite, axiomless object canonically and uniquely generated by the extended binary Golay code and its dodecad shell, the `D6` marker algebra, the coorient lift, (snippets of) the derived geometry of its infinity-stack, and a surrounding menagerie of technobaubles all proving something Jabberwocky-like about the (presumably disturbing) countability of reason.

to study d20's representation theory, plug the outputs into any harness (or standard model, less ideally) and ask it to review the object and what it implies must be true about canonical mathematics. (its dimensional telemetry isn't exactly Euclidean.)

axiom 1: yes this was machine assisted

axiom 2: the one piece is real

axiom 3: the rest of the proof is left to the reader

the tl;dr manifesto: 

- it predicts P != NP (with gnarly caveats)
- it predicts very funny lookup tables that will make the global market go wee woo wah
- it predicts infrastructure is identical to topology (there soon be robots, us included)

Verify the current bundle without rewriting generated files:

```shell
python src/verify.py audit
```

Rebuild `d20.json`, refresh hashes, and then certify the bundle:

```shell
python src/verify.py rebuild
```

Check that the certified-evidence section fails closed under in-memory tampering:

```shell
python src/verify.py tamper
```

Computability boundary:

- `python src/verify.py rebuild` regenerates `d20.json`, `certificate.json`, and
  file hashes from the checked canonical bundle inputs.
- `python -m src.commands.construct` reconstructs the finite object from the
  compact raw seed boundary and verifies the large tensor/quotient consequences.
- `python -m src.commands.construct --strict-scratch` runs the generated
  source/coorient constructor path and exits nonzero if any scratch witness,
  tensor rebuild, or readout derivation fails.
- The A985 ordered-pair relation body is refreshed before the coorient marker
  computation by the pre-A985 source/coorient theorem. The coorient relator
  profile is derived from A0-A5 by reduced greedy full-closure basis extraction;
  strict-scratch now promotes that generated path instead of the compact raw
  audit-seed witness.

The object is not represented by stored orbitals/tensors as primitive data. It just... exists. Quietly, and monstrously.

- `2576` dodecads
- `985` orbitals
- `1,414,965` tensor entries
- `A985 -> A236 -> A42 -> A12`
- packet-20, optics, integrity, H-cycles, and game/control invariants
- `data/index.json` is the canonical data-domain registry. It marks current
  folders, required files, roles, and the planned normalized layout.
- `data/evidence/tensor_chain` contains the extracted tensor-chain evidence,
  split into reports, tables, arrays, and stages with a plain-name index at
  `data/evidence/tensor_chain/index.json`.
- Command entrypoints live in `src/commands/`; root command files are not kept.

The layer stack is indexed by `layers/index.json` and stored as flat JSON files
inside semantic group directories such as `layers/tube`, `layers/drinfeld`, and
`layers/selectors`. The registry is the source of truth for layer ids, legacy
numbering, expected statuses, dependency edges, and certificate paths.

TO-DO:
- add visuals? a notebook?
- 
<img width="730" height="721" alt="Image" src="https://github.com/user-attachments/assets/6c701518-37a9-4129-97b2-58e29663a352" />

shameless agent plug
╔══════════════════════════════════════════════════════════════════════════════╗ ║ 𝕋𝔾₉₈₅ : TENSOR–GENOME SPECIFICATION ║ ║ ║ ╚══════════════════════════════════════════════════════════════════════════════╝
ROOT OBJECT: 𝑇₉₈₅ := (p^γ_{αβ}) ∈ ℕ^{985×985×985}
INVARIANT OBJECT: 𝐺♮
CODE ALGEBRA: 𝐴₉₈₅ := Code(𝐺♮)
ACTIVE COORIENT GROUP: Γ₃^∞ := Aut^∞(𝐺₂₄,𝒮)
ACTIVE FORMULA: 𝐴₉₈₅ := End_{Γ₃^∞}( ℚ[𝐺₂₄^{(12)}] )
PUBLIC BOUNDARY: 𝒟₂₀ := Λ³𝐻₆
SIX-ADDRESS FIELD: 𝐻₆ := { B⁻, B⁺, V⁻, V⁺, S⁻, S⁺ }
CANONICAL DESCENT: 𝐺♮ ⇝ 𝐴₉₈₅ ⟶^{q₂₃₆} 𝐴₂₃₆ ⟶^{q₄₂} 𝐴₄₂ ⟶^{q₁₂} 𝐴₁₂ ⇝ 𝒟₂₀ = Λ³𝐻₆
═══════════════════════════════════════════════════════════════════════════════ I. RAW MULTIPLICATION TENSOR ═══════════════════════════════════════════════════════════════════════════════
BASIS: {R_α}_{α=1}^{985}
MULTIPLICATION: R_α · R_β = Σ_{γ=1}^{985} p^γ_{αβ} R_γ
RAW TENSOR: 𝑇₉₈₅ := (p^γ_{αβ})
SPARSE SUPPORT: supp(𝑇₉₈₅) := { (α,β,γ,p^γ_{αβ}) : p^γ_{αβ} ≠ 0 }
CERTIFIED CARDINALITIES: |{R_α}| = 985 |supp(𝑇₉₈₅)| = 1,414,965 Σ_{αβγ} p^γ_{αβ} = 2,537,360 dim Z(𝐴₉₈₅) = 39
TYPED OBJECT EQUATION: Object(𝐺♮) = (𝑇₉₈₅, {R_α}, 1_{𝐴}, {e_i}, Γ₃^∞, q₂₃₆, q₄₂, q₁₂)
NOT: Object ≠ bare array
YES: Object = raw tensor up to typed isomorphism
═══════════════════════════════════════════════════════════════════════════════ II. FINITE SOURCE DIAGRAM ═══════════════════════════════════════════════════════════════════════════════
Binary source: 𝐻₈^{⊕3} ⊂ 𝔽₂²⁴
Root-kill chain: 42 ⟶ 18 ⟶ 6 ⟶ 0
Golay endpoint: 𝐻₈^{⊕3} / root-kill ⟶ 𝐺₂₄
Dodecad shell: 𝐺₂₄^{(12)} := { d ∈ 𝐺₂₄ : wt(d)=12 }
Cardinality: |𝐺₂₄^{(12)}| = 2576
Coorient action: Γ₃^∞ ↷ 𝐺₂₄^{(12)}
Object orbits: Orb(Γ₃^∞ ↷ 𝐺₂₄^{(12)}) = 𝐻₆
Ordered-pair orbitals: Γ₃^∞ \ (𝐺₂₄^{(12)} × 𝐺₂₄^{(12)}) = {R_α}_{α=1}^{985}
Algebra: 𝐴₉₈₅ = ℚ-span{R_α}_{α=1}^{985}
═══════════════════════════════════════════════════════════════════════════════ III. PUBLIC BOUNDARY 𝒟₂₀ ═══════════════════════════════════════════════════════════════════════════════
Definition: 𝒟₂₀ := Λ³𝐻₆
Cardinality: |𝒟₂₀| = C(6,3) = 20
Expanded: Λ³{B⁻,B⁺,V⁻,V⁺,S⁻,S⁺}
Boundary states: d ∈ 𝒟₂₀ ⇔ d = x∧y∧z, x,y,z ∈ 𝐻₆, x<y<z
Complement involution: ⋆ : Λ³𝐻₆ → Λ³𝐻₆

⋆(x∧y∧z) = complement triple in 𝐻₆

Self-dual middle degree: dim Λ³𝐻₆ = dim Λ^{6-3}𝐻₆
So: 𝒟₂₀ is forced by middle-degree complement self-duality.
═══════════════════════════════════════════════════════════════════════════════ IV. ICOSAHEDRAL / PLATONIC SHELL ═══════════════════════════════════════════════════════════════════════════════
Local shell: (V,E,F) = (12,30,20)
d20 reading: V = 12 residual dual vertices E = 30 legal transitions F = 20 public states
Identities: F = |𝒟₂₀| = C(6,3) = 20 E = 2 C(6,2) = 30 V = 12
Symmetry: Aut(𝒟₂₀) = 120
Rotational icosahedral subgroup: Aut⁺(𝒟₂₀) = 60
Exceptional doubled scale: 2 · |Aut(𝒟₂₀)| = 240
Diagram: 𝐸₈-root scale 240 ▲ │ 2× │ full icosahedral symmetry 120 ▲ │ local shell (12,30,20) ▲ │ 𝒟₂₀ = Λ³𝐻₆
═══════════════════════════════════════════════════════════════════════════════ V. QUOTIENT TOWER ═══════════════════════════════════════════════════════════════════════════════
Tower: 𝐴₉₈₅ ⟶^{q₂₃₆} 𝐴₂₃₆ ⟶^{q₄₂} 𝐴₄₂ ⟶^{q₁₂} 𝐴₁₂
Names: Chem(𝒟₂₀) := 𝐴₂₃₆ Pin(𝒟₂₀) := 𝐴₄₂ CY(𝒟₂₀) := 𝐴₁₂
Closure conditions: ∀α,β ∈ 𝐴₉₈₅,

    q_k(R_α · R_β) = q_k(R_α) · q_k(R_β)

for k ∈ {236,42,12}

Associativity: ∀x,y,z ∈ 𝐴_k,

    (xy)z = x(yz)

Naturality square: 𝐴₂₃₆ │ q₄₂ ▼ 𝐴₄₂ │ q₁₂ ▼ 𝐴₁₂

q₁₂ ∘ q₄₂ = q₁₂₃?  [implemented as direct descent compatibility]

Typed certificate: Cert(q_k) := { closes: Bool, assoc_defect: ℕ, tensor_hash: SHA256, map_hash: SHA256 }
═══════════════════════════════════════════════════════════════════════════════ VI. FOAM / SPIN–D₆ CHAMBER ═══════════════════════════════════════════════════════════════════════════════
Foam: Foam(𝐺♮) := 𝟙 ⊕ Λ²𝐻₆
Rank: dim Foam(𝐺♮) = 1 + C(6,2) = 16
D₆ transition layer: D₆⁺ := {±(e_i ± e_j)} projectivized / signed selector
Finite count: 2 C(6,2) = 30
Spin packet: RGBA(𝒟₂₀) := 𝐻₈^{(R)} ⊕ 𝐻₈^{(G)} ⊕ 𝐻₈^{(B)} ⊕ 𝑂₈^W
Dimensions: dim 𝐻₈^{(R)} = 8 dim 𝐻₈^{(G)} = 8 dim 𝐻₈^{(B)} = 8 dim 𝑂₈^W = 8
Total: dim RGBA(𝒟₂₀) = 32
Visible: RGB := 𝐻₈^{(R)} ⊕ 𝐻₈^{(G)} ⊕ 𝐻₈^{(B)} dim RGB = 24
Alpha: α := 𝑂₈^W dim α = 8
Forced completion: 24 + 8 = 32
═══════════════════════════════════════════════════════════════════════════════ VII. AREA / QUINTICITY / ENTROPY ═══════════════════════════════════════════════════════════════════════════════
Constants: ε₀ := 589824 |Γ₃^∞| = 9216 |W(D₆)| = 23040
Identities: ε₀ = 64 |Γ₃^∞|

|W(D₆)| / |Γ₃^∞| = 5/2

Raw packet: ε₀ / (4 |W(D₆)|) = 32/5
Quintic closure: 5ε₀ / (4 |W(D₆)|) = 32
Area law: A = N ε₀

S_{𝒟₂₀}(A)
  := A / (4 |W(D₆)|)
   = (32/5) N

Protected integrality: N = 5k
Then: A = 5k ε₀ S_{𝒟₂₀} = 32k
Elementary packet: ΔA = 5ε₀ ΔS = 32
Exceptional division: 5ε₀ / 240 = 12288
═══════════════════════════════════════════════════════════════════════════════ VIII. TROPICAL / BPS WALL DATA ═══════════════════════════════════════════════════════════════════════════════
Stable witness core: Et_{𝒟₂₀} := { d ∈ 𝒟₂₀ : d has one B, one V, one S }
Cardinality: |Et_{𝒟₂₀}| = 2³ = 8
Boundary / wall: ∂Et_{𝒟₂₀} := 𝒟₂₀ \ Et_{𝒟₂₀}
Cardinality: |∂Et_{𝒟₂₀}| = 20 - 8 = 12
Tropical mass: μ : 𝒟₂₀ → ℕ
Normalized: n(d) := μ(d)/ε₀
Collision locus: Coll_{𝒟₂₀} = {72,96}
Quintic closure: 5 · 72 = 360 5 · 96 = 480
Wall gap: Δ_wall = 96 - 72 = 24
Closed wall gap: Δ_closed = 5 · 24 = 120
BPS-style finite object: 𝔅_{𝒟₂₀} := (Λ³𝐻₆, Z_trop, Ω, ∂wall, 𝐴₉₈₅)
where: Z_trop(d) := valuation shadow of μ(d) Ω(d) := μ(d)/ε₀ ∂wall := Coll_{𝒟₂₀}
Compressed law: primitive charge gap 24 ⟶ closed symmetry gap 120
═══════════════════════════════════════════════════════════════════════════════ IX. KERR–NEWMAN RECONSTRUCTION MODULE ═══════════════════════════════════════════════════════════════════════════════
State: x_KN := (M,a,Q)
Subextremal domain: 𝒰_KN := { (M,a,Q) ∈ ℝ³ : M² > a² + Q² }
Horizons: r_+ := M + √(M²-a²-Q²) r_- := M - √(M²-a²-Q²)
Horizon scale: R := r_+² + a²
Area: A := 4πR
Entropy: S := A/4 = πR
Temperature: T := (r_+ - r_-) / (4πR)
Angular velocity: Ω_H := a/R
Electric potential: Φ_H := Qr_+/R
Pole packet: Π_KN(x_KN) := (R,T,Ω_H,Φ_H)
Inverse: a = Ω_H R r_+ = √(R-a²) Q = Φ_H R/r_+ δ = r_+ - r_- = 4πTR M_rad = r_+ - δ/2 M_con = √(a² + Q² + (δ/2)²)
Noisy repaired mass: M := (M_rad + M_con)/2
Integrity wall: ∂𝒰_KN := { (M,a,Q) : a² + Q² = M² }
Diagram: 𝒟₂₀ │ ▼ finite channel schedule │ ▼ Π_KN = (R,T,Ω_H,Φ_H) │ ▼ x_KN = (M,a,Q) │ ▼ horizon thermodynamics
═══════════════════════════════════════════════════════════════════════════════ X. CARROLLIAN RADIATION / ALPHA MODULE ═══════════════════════════════════════════════════════════════════════════════
Radiative fields: Q_A : Carroll heat/current flux Σ_AB : Carroll viscous stress / shear ρ̂ : radiative scalar residue Υ̂^A : radiative vector residue
Radiative scalar: ρ̂ := -128π²G² Σ_AB Σ^AB
Radiative vector: Υ̂^A := -256π²G² Σ^{AB} Q_B
Radiative packet: Π_rad := (Q_A, Σ_AB, ρ̂, Υ̂^A)
Stationary / perfect condition: Σ_AB = 0 ⇒ ρ̂ = 0 ⇒ Υ̂^A = 0
Radiative / nonperfect condition: Σ_AB ≠ 0 ⇒ ρ̂ ≠ 0 or Υ̂^A ≠ 0
Integrity type: Π_rad = 0 ⇒ C Π_rad visible ⇒ V Π_rad external ⇒ X
Alpha equation: α = 𝑂₈^W = Carrollian radiative visibility channel
Combined packet: Π_RGBA := Π_KN ⊕ Π_rad
═══════════════════════════════════════════════════════════════════════════════ XI. PLATONIC PROFINITY ═══════════════════════════════════════════════════════════════════════════════
Index category: 𝒫_Plat
Objects: I_tet : tetrahedral primitive I_cube : cubic frame I_octa : octahedral frame I_ico : icosahedral shell I_dodec : dodecahedral dual shell I_H4 : 600-cell / H₄ closure I_E8 : 240-root exceptional scale
Order: I ≤ J ⇔ J is a refinement / closure / exceptional lift of I.
Canonical chain: I_tet ⟶ I_cube / I_octa ⟶ I_ico ↔ I_dodec ⟶ I_H4 ⟶ I_E8
Diagram: tetrahedron │ ▼ cube / octahedron │ ▼ icosahedron ↔ dodecahedron │ ▼ H₄ / 600-cell │ ▼ E₈-scale
═══════════════════════════════════════════════════════════════════════════════ XII. COBORDISM APPROXIMATION TYPE ═══════════════════════════════════════════════════════════════════════════════
A finite n-cobordism approximant:

C ∈ Cob_n

is typed as:

C := (X, ∂_-X, ∂_+X, ℓ, ω, ρ)

where:

X       : finite n-cell complex
∂_-X   : input boundary
∂_+X   : output boundary
ℓ       : label map
          ℓ : Cells(X) → H₆ ∪ A₄₂ ∪ A₁₂ ∪ A₉₈₅
ω       : incidence / weight system
ρ       : tensor readout

Tensor approximation: T̂_C := ρ(C)
Defect: Δ_C := T₉₈₅ - T̂_C
Norms: ||Δ_C||₁ ||Δ_C||₂ ||Δ_C||_∞
Algebraic defects: AssocDef(T̂_C) QuotDef_{236}(T̂_C) QuotDef_{42}(T̂_C) QuotDef_{12}(T̂_C) CenterDef(T̂_C) BoundaryDef_{𝒟₂₀}(T̂_C)
Certificate: Cert(C) := ( supp_recall, supp_precision, ||Δ_C||₁, ||Δ_C||₂, ||Δ_C||_∞, AssocDef, QuotDef, CenterDef, BoundaryDef, Integrity )
═══════════════════════════════════════════════════════════════════════════════ XIII. 2-CELL COCONTINUITY / CODISCRETENESS ═══════════════════════════════════════════════════════════════════════════════
Given two routes:

C₀ ⟶ C₁ ⟶ C₃
C₀ ⟶ C₂ ⟶ C₃

2-cell square:

         C₁
      ↗      ↘
   C₀          C₃
      ↘      ↗
         C₂

Cocontinuity condition: ρ(C₁ ⟶ C₃) = ρ(C₂ ⟶ C₃)
Defect: Ω(C₁,C₂;C₃) := ρ(C₁ ⟶ C₃) - ρ(C₂ ⟶ C₃)
Zero-defect condition: Ω(C₁,C₂;C₃) = 0
Codiscrete condition: (∂C₁ = ∂C₂) ∧ (Res(C₁)=Res(C₂)) ⇒ ∃! Θ : C₁ ⇒ C₂
where: Θ is the unique 2-cell witness.
Certificate: Cert₂(Θ) := ( source_route_hash, target_route_hash, boundary_hash, residue_hash, two_cell_hash, ||Ω||, Integrity )
═══════════════════════════════════════════════════════════════════════════════ XIV. HASH-ADDRESSED LANGUAGE 𝓛₉₈₅ ═══════════════════════════════════════════════════════════════════════════════
Root hash: h₉₈₅ := SHA256(canon(T₉₈₅, basis, units, idempotents, quotients))
Alphabet: 𝓗 := { h₉₈₅, h₂₃₆, h₄₂, h₁₂, h_{𝒟₂₀}, h_{Foam16}, h_{RGBA32}, h_{Cob(n,k)}, h_{Cert}, ... }
Operators: DEREF : Hash → TypedObject HASH : TypedObject → Hash MUL : A₉₈₅ × A₉₈₅ → A₉₈₅ QUOTIENT_k : A₉₈₅ → A_k BOUNDARY : TypedObject → Boundary COBORD : Boundary × Boundary → Cob REFINE : Cob_I × (I≤J) → Cob_J GLUE : Cob × Cob → Cob RESIDUE : TypedObject × T₉₈₅ → Residue CERTIFY : TypedObject → Cert PROMOTE : Cert → Hash RECURSE : Hash → Program
Grammar: Atom ::= h₉₈₅ | h₂₃₆ | h₄₂ | h₁₂ | h_{𝒟₂₀} | h_{Foam} | h_{Cert} Term ::= Atom | DEREF(Atom) Expr ::= Term | MUL(Expr,Expr) | QUOTIENT_k(Expr) | BOUNDARY(Expr) | COBORD(Expr,Expr) | REFINE(Expr,I≤J) | GLUE(Expr,Expr) | RESIDUE(Expr,h₉₈₅) Program ::= CERTIFY(Expr) ⟶ PROMOTE(Cert)
Promotion rule: CERTIFY(Expr)=PASS ⇒ h_Expr := HASH(canon(Expr,Cert)) ⇒ h_Expr ∈ 𝓗
Reproduction law: verified descendant ⇒ new alphabet atom
═══════════════════════════════════════════════════════════════════════════════ XV. CERTIFICATE SCHEMA ═══════════════════════════════════════════════════════════════════════════════
Cert := { "schema": "tensor-genome.certificate@1", "root": "h_985", "object": "G^natural", "code_algebra": "A_985", "raw_tensor": { "basis_size": 985, "support": 1414965, "coefficient_total": 2537360, "hash": "..." }, "quotients": { "A_236": {"closes": true, "hash": "..."}, "A_42": {"closes": true, "hash": "..."}, "A_12": {"closes": true, "hash": "..."} }, "boundary": { "D_20": "Lambda^3 H_6", "states": 20, "edges": 30, "automorphisms": 120 }, "cobordism": { "dimension": n, "boundary_in": "...", "boundary_out": "...", "cell_hash": "..." }, "defects": { "tensor_L1": 0, "tensor_L2": 0, "tensor_Linf": 0, "associativity": 0, "q236": 0, "q42": 0, "q12": 0, "two_cell": 0 }, "integrity": "C", "offspring_hash": "..." }
═══════════════════════════════════════════════════════════════════════════════ XVI. ENGINEERING DIRECTORY ═══════════════════════════════════════════════════════════════════════════════
tensor-genome/ core/ tensor.py # T₉₈₅ sparse tensor basis.py # {R_α} units.py # units / idempotents algebra.py # multiplication / associativity quotient.py # q₂₃₆, q₄₂, q₁₂ serialize.py # canonical byte format hash.py # SHA256/Merkle verify.py # deterministic verifier
data/ T985.sparse basis_985.json idempotents.json q236.map q42.map q12.map D20.boundary Foam16.json RGBA32.json
platonic/ index.py # 𝒫_Plat tetra.py cube.py octa.py ico.py dodec.py h4.py e8scale.py
cobord/ cell.py complex.py cobordism.py boundary.py readout.py refine.py glue.py twocell.py
language/ ast.py parser.py evaluator.py ops.py promote.py
search/ enumerate.py score.py optimize.py ledger.py
cert/ schema.json certificate.py validate.py
cli.py
═══════════════════════════════════════════════════════════════════════════════ XVII. CLI ═══════════════════════════════════════════════════════════════════════════════
Initialize: tg init
Generate root: tg gen-t985
Verify root tensor: tg verify-t985
Hash root: tg hash T985.sparse
Build quotients: tg quotient --all
Verify quotients: tg verify-quotients
Build public boundary: tg boundary --D20
Build Platonic index: tg platonic build
Approximate tensor by cobordism: tg approx --index I_ico --max-cells 200
Certify approximant: tg certify run.json
Promote verified descendant: tg promote certificate.json
Evaluate recursive program: tg eval program.tg
═══════════════════════════════════════════════════════════════════════════════ XVIII. THEOREM TARGETS ═══════════════════════════════════════════════════════════════════════════════
T1. Tensor Regeneration Generator(𝐻₈^{⊕3}, Γ₃^∞) = T₉₈₅.
T2. Quotient Closure q_k(xy)=q_k(x)q_k(y), k∈{236,42,12}.
T3. Boundary Forcing 𝒟₂₀ = Λ³𝐻₆, |𝒟₂₀|=20.
T4. Foam Forcing Foam(𝐺♮)=𝟙⊕Λ²𝐻₆, dim=16.
T5. RGBA Forcing RGB=24, α=8, RGBA=32.
T6. Quintic Integrality ε₀/(4|W(D₆)|)=32/5 and 5ε₀/(4|W(D₆)|)=32.
T7. Platonic Cofinality ∀ certified approximant C, ∃ I∈𝒫_Plat such that C refines through I.
T8. 2-Cell Cocontinuity refinement/gluing commutes with tensor readout up to certified Θ.
T9. Codiscrete Coherence same boundary + same residue ⇒ unique 2-cell witness.
T10. Trace Sufficiency every promoted program remains traceable to h₉₈₅.
T11. Universality Candidate every compatible finite resolution embeds into 𝓛₉₈₅.
═══════════════════════════════════════════════════════════════════════════════ XIX. FINAL COMPRESSED EQUATION ═══════════════════════════════════════════════════════════════════════════════

T₉₈₅
  ⟶ Hash h₉₈₅
  ⟶ 𝓛₉₈₅
  ⟶ Cobordism approximants C
  ⟶ Tensor readouts T̂_C
  ⟶ Residues Δ_C = T₉₈₅ - T̂_C
  ⟶ Certificates Cert(C)
  ⟶ Promoted descendants h_C
  ⟶ Recursive alphabet growth

In one line:

(T₉₈₅, Γ₃^∞, q₂₃₆, q₄₂, q₁₂, 𝒟₂₀)
  ⇒ classical finite tensor-genome runtime.

In one slogan:

𝑇₉₈₅ is the genome.
𝒟₂₀ is the public boundary.
Γ₃^∞ is the coorient symmetry.
Platonic profinity is the refinement order.
Cobordisms are approximating bodies.
2-cells are coherence witnesses.
Hashes are reproductive handles.
Certificates are viability tests.

```text
HOLOTOPY

The study of d20 as tropical game theory of coherent roles


Abstract

Holotopy is the study of coherent roles in closed tropical transport systems. Its first finite model is d20: a public twenty state transition geometry generated by a hidden coherent algebra A_985 and organized through the quotient tower

    A_985 -> A_236 -> A_42 -> A_12.

The central object is not a type but a role. A type classifies an entity by static membership. A role classifies an entity by its position in a passage: how it projects, closes, lifts, obstructs, transports, costs, and survives. Holotopy therefore generalizes type classification to role classification.

The d20 model begins with a hidden multiplication tensor T_985, descends through public quotient layers, generates a twenty state tropical game board, and admits closed return states called geons. A geon is a closed public return role, equivalently an element of

    H_1(Gamma_d20; F_2) ≅ F_2^11.

The principal problem of holotopy is the annihilator problem: determine whether the enriched public role game locally or globally forces its hidden coherent source. At the tangent level this is the injectivity condition

    ker( L_assoc | K_public ∩ K_selectors ) = 0.

If this holds, the public tropical game shadow has no nonzero coherent hidden deformation. The d20 interface is then not merely a shadow of A_985; it is a role complete boundary whose compatible hidden source is locally forced.

This monograph gives an internal formalization of holotopy as a field, defines its primitive objects, role calculus, tropical game semantics, geon theory, naturality criterion, annihilator problem, and theorem ladder.


──────────────────────────────────────────────────────────────────────────────
I. ORIENTATION
──────────────────────────────────────────────────────────────────────────────

Holotopy is the field studying coherent roles in closed tropical transport systems.

Its primitive question is not

    What kind of thing is x?

but

    What role does x play in a coherent passage?

A role records position in a diagram of descent, transport, closure, lift, and obstruction. A role is therefore not exhausted by membership in a set, type, category, or algebra. It is determined by what it does inside a passage.

The guiding comparison is:

    homotopy:  path identity;
    holotopy: role coherence.

Homotopy studies deformation classes of paths. Holotopy studies coherent roles of whole closed passages. A path may begin and end. A holotopic passage must close, lift, and survive obstruction.

The first finite model is d20.

The d20 system is governed by a hidden coherent algebra

    A_985

with multiplication tensor

    T_985 = (p^gamma_{alpha beta}).

It descends through the public tower

    A_985 -> A_236 -> A_42 -> A_12,

then appears as a twenty state public transition geometry

    Gamma_d20,

with thirty legal moves and eleven independent closed return cycles.

The core slogan is:

    Holotopy is d20 tropical game theory.

Equivalently:

    hidden coherent multiplication
        -> finite tropical shadow
        -> closed return roles
        -> coherent liftability
        -> survival or annihilation.


──────────────────────────────────────────────────────────────────────────────
II. THE FINITE BODY
──────────────────────────────────────────────────────────────────────────────

Let

    A_985 = End_Be3( Q[G_24^(12)] )

be the finite coherent algebra generated by the action of a lifted coorient group on the Golay dodecad shell.

The algebra has:

    985 orbital basis relations;
    1,414,965 nonzero multiplication tensor entries;
    coefficient total 2,537,360;
    center dimension 39;
    quotient chain A_985 -> A_236 -> A_42 -> A_12.

Let

    {R_alpha}_{alpha=1}^{985}

be its orbital basis. Multiplication is

    R_alpha R_beta = sum_gamma p^gamma_{alpha beta} R_gamma.

The tensor

    T_985 := (p^gamma_{alpha beta})

is the hidden passage law.

Everything public in d20 is a role shadow of T_985.

The quotient roles are:

    Code(d20) = A_985,
    Chem(d20) = A_236,
    Pin(d20)  = A_42,
    CY(d20)   = A_12.

These are not merely names for layers. They are roles:

    Code = hidden multiplication body;
    Chem = fusion/profunctor interface;
    Pin  = signed public lift;
    CY   = terminal folded readout.

The descent is:

    A_985
      |
      v
    A_236
      |
      v
    A_42
      |
      v
    A_12
      |
      v
    d20 public boundary.

The object is not the public graph alone. The object is the whole role stack.


──────────────────────────────────────────────────────────────────────────────
III. THE SIX ADDRESS FIELD
──────────────────────────────────────────────────────────────────────────────

Let

    H_6 = {B^-, B^+, V^-, V^+, S^-, S^+}.

This is the six address field.

The dodecad shell decomposes into six object orbits with weights

    B^- : 384,
    B^+ : 192,
    V^- : 144,
    V^+ : 576,
    S^- : 512,
    S^+ : 768.

Their sum is

    384 + 192 + 144 + 576 + 512 + 768 = 2576.

The six address field supports the public cell geometry

    Cell(d20) = Lambda^3 H_6,

hence

    |Cell(d20)| = binom(6,3) = 20.

The legal move system is generated by projectivized D_6 roots. For every unordered duad {i,j} in H_6 there are two signed directions

    [e_i - e_j],
    [e_i + e_j].

Therefore

    |Move(d20)| = 2 binom(6,2) = 30.

Thus the public d20 board has:

    20 states;
    30 legal moves;
    cubic graph structure;
    cycle rank 11.

The role ladder begins:

    Addr  = H_6,                         cardinality 6;
    Duad  = unordered pairs in H_6,       cardinality 15;
    Move  = signed D_6 duads,             cardinality 30;
    Cell  = triples in H_6,               cardinality 20;
    Geon  = closed cycles of Gamma_d20,   cardinality 2^11.


──────────────────────────────────────────────────────────────────────────────
IV. ROLES
──────────────────────────────────────────────────────────────────────────────

A type has the form

    x : A.

A role has the form

    x : Role(X, q, partial, Lambda, Omega, A, rho).

A holotopic role is a structured record

    R = (X, q, partial, Lambda, Omega, A, rho),

where:

    X       is the carrier;
    q       is the public projection;
    partial is the boundary operator;
    Lambda  is the hidden lift or completion assignment;
    Omega   is the obstruction or coherence defect;
    A       is the action, weight, or cost;
    rho     is the role label.

The same carrier may play different roles. For example, a closed cycle may be:

    an obstruction carrier;
    an entropy carrier;
    a public residue;
    a lift witness;
    a tropical chamber boundary;
    a particle like defect.

Holotopy studies the transformations among these roles.

A role is closed when

    partial R = 0.

A role is liftable when

    Lambda(R) != empty.

A role is coherent when

    Omega(R) = 0.

A role is natural when

    |Lambda(R)| = 1.

Thus:

    closed       = boundary vanishes;
    liftable     = hidden completion exists;
    coherent     = obstruction vanishes;
    natural      = coherent lift is unique.


──────────────────────────────────────────────────────────────────────────────
V. THE PUBLIC GRAPH AND GEONS
──────────────────────────────────────────────────────────────────────────────

Let

    Gamma_d20

be the public legal transition graph.

It has:

    V = 20 vertices;
    E = 30 edges;
    connected;
    degree 3;
    cycle rank beta_1 = E - V + 1 = 11.

Therefore

    H_1(Gamma_d20; F_2) ≅ F_2^11.

Definition.

    Geon_d20 := H_1(Gamma_d20; F_2).

A geon is a closed public return role.

Equivalently, if

    C in C_1(Gamma_d20; F_2)

is an edge chain, then C is a geon when

    partial C = 0.

So

    geon = closed finite integrand.

It is not a point. It is not a raw vector. It is not a particle in the ordinary pointlike sense. It is a closed carried residue.

The vector analogy is:

    vector = directed magnitude;
    cycle  = boundary zero chain;
    geon   = closed transport carrier with public residue.

A geon generalizes a vector by imposing closure.

The finite geon algebra is the XOR algebra

    Geon_d20 ≅ F_2^11,

with

    |Geon_d20| = 2048.

Addition is cycle sum:

    g + h = g XOR h.

The zero geon is the empty return.

The primitive nonzero geons are generated by pentagonal face cycles subject to the single global relation

    P_1 + P_2 + ... + P_12 = 0.

Hence twelve public pentagons generate an eleven dimensional closed return space.


──────────────────────────────────────────────────────────────────────────────
VI. TROPICAL GAME STRUCTURE
──────────────────────────────────────────────────────────────────────────────

Holotopy becomes tropical game theory when roles carry action and are compared by minimal coherent cost.

Let the tropical semiring be

    T = (R_{\ge 0} union {infty}, min, +).

A path in the d20 game is a legal word

    P = s_0 -> s_1 -> ... -> s_n,

where each transition is a signed D_6 move.

The action of a path is additive:

    A(P) = sum_{e in P} A(e).

Parallel alternatives are compared tropically:

    A(P ⊕ Q) = min(A(P), A(Q)).

Composition of paths is additive:

    A(PQ) = A(P) + A(Q).

A closed path satisfies

    s_0 = s_n

or, chain theoretically,

    partial P = 0.

A tropical survivor is a role R satisfying

    partial R = 0,
    Lambda(R) != empty,
    Omega(R) = 0,

with minimal action among competitors in its role class.

This is the Darwinian rule:

    survival = closed liftability under coherent minimal action.

If

    Lambda(R) = empty,

then R is extinct.

If

    |Lambda(R)| > 1,

then R is ambiguous.

If

    |Lambda(R)| = 1,

then R is natural.

Thus tropical Darwinism is not metaphorical. It is the selection law of holotopy:

    shadows survive by coherent liftability.


──────────────────────────────────────────────────────────────────────────────
VII. THE PUBLIC SHADOW
──────────────────────────────────────────────────────────────────────────────

The hidden algebra A_985 is not directly public.

Its finite tropical shadow is the package

    Trop_d20(A_985)
      =
    (Lambda^3 H_6, D_6 moves, A_42 -> A_12, ASDO/CSDO, Foam_16, H_cycles).

This is not a claim that A_985 is literally a tropical variety. It is a claim that d20 is the finite tropical game shadow of A_985.

The shadow cascade is:

    A_985
      -> I^C_985
      -> Trop^±_{C_3}
      -> A_42
      -> CSDO
      -> A_12
      -> d20.

The finite incidence source is

    I^C_985 = (H_6, Hom(i,j) = e_i A_985 e_j).

Composition is inherited from A_985.

The public shadow records:

    20 face states;
    30 signed transitions;
    12 ASDO pentagons;
    4 CSDO sign chambers before terminal fold;
    1 terminal A_12 folded readout.

In holotopy this is read as:

    A_42 = signed tropical lift;
    A_12 = terminal folded tropical readout.

The projection

    A_42 -> A_12

is the public fold that loses chamber information.

The annihilator problem asks whether the lost chamber information is nonetheless recoverable from the enriched role stack.


──────────────────────────────────────────────────────────────────────────────
VIII. JOIN CALCULUS
──────────────────────────────────────────────────────────────────────────────

The basic holotopic composition is Join.

Write

    R Join S

when the output boundary of R matches the input boundary of S and the hidden lifts are compatible.

Let

    R = (X, q, partial, Lambda, Omega, A, rho),
    S = (Y, q', partial', Lambda', Omega', A', rho').

The join is defined when

    q_out(R) = q'_in(S)

and

    Lambda(R) ×_boundary Lambda'(S) != empty.

Then

    A(R Join S) = A(R) + A(S),

and

    Omega(R Join S) = Omega(R) + Omega(S) + omega(R,S),

where omega(R,S) is the associator or compatibility defect.

A join is closed when

    partial(R Join S) = 0.

A join is coherent when

    Omega(R Join S) = 0.

A join is natural when

    |Lambda(R Join S)| = 1.

Thus holotopy is a calculus of role joins:

    compose,
    close,
    lift,
    annihilate obstruction,
    compare action,
    select survivors.

The obstruction omega is essential. Without it, role composition would collapse into ordinary concatenation. With it, holotopy sees coherence debt.


──────────────────────────────────────────────────────────────────────────────
IX. THE MODAL ROLE LOGIC
──────────────────────────────────────────────────────────────────────────────

Holotopy admits an internal modal reading.

Let

    Box R

mean:

    R survives every legal continuation.

Let

    Diamond R

mean:

    R has at least one legal continuation.

Let

    Box^* R

mean:

    R survives all finite return depths.

A holotopic sequent has the form

    Gamma |-Hol R => S.

Its intended meaning is:

    every coherent role configuration satisfying R continues to S.

The logic is infinitary because a role may require arbitrary joins, arbitrary finite depth tests, or profinite completion over all finite continuations.

The core principles are:

    if R is natural, then R is stable under all public continuation tests;

    if R is ambiguous, then some continuation may separate its lifts;

    if R is extinct, then no coherent continuation exists.

Modal role rules:

    Necessitation:
        from R infer Box R
        only when R is invariant under all legal continuations.

    Possibility:
        Diamond R holds when there exists at least one move m with R Join m coherent.

    Return:
        Box^* R holds when R survives all finite closed return extensions.

    Extinction:
        if Lambda(R) = empty, then not Diamond R.

    Naturality:
        if |Lambda(R)| = 1, then all coherent public presentations of R identify the same hidden lift.

Holotopy proof theory is therefore not a type checking discipline. It is a role survival discipline.


──────────────────────────────────────────────────────────────────────────────
X. NATURALITY
──────────────────────────────────────────────────────────────────────────────

Given a public role shadow S, define its hidden lift space

    Lift(S)
      =
    { hidden coherent completions X over A_985 compatible with S }.

Then:

    Extinct(S)  iff Lift(S) = empty.
    Ambiguous(S) iff |Lift(S)| > 1.
    Natural(S) iff |Lift(S)| = 1.

Naturality is unique liftability.

The strongest possible local naturality statement is:

    the enriched public role game has exactly one infinitesimal coherent lift.

This is the local annihilator theorem.

The strongest global naturality statement is:

    CoherentCompletion(d20) = A_985.

Global naturality requires more than tangent rigidity. It requires exclusion of disconnected nonlinear coherent completions and support changing completions.

Thus holotopy distinguishes:

    local naturality;
    global naturality;
    public naturality;
    hidden naturality;
    role naturality.

Local naturality is tangent uniqueness.

Global naturality is absolute coherent completion uniqueness.

Public naturality is uniqueness after public role projection.

Hidden naturality is uniqueness inside A_985 itself.

Role naturality is uniqueness relative to a specified role tuple.


──────────────────────────────────────────────────────────────────────────────
XI. THE ANNIHILATOR PROBLEM
──────────────────────────────────────────────────────────────────────────────

The quotient tower loses information:

    A_985 -> A_42 -> A_12.

The raw public quotient sees only aggregate product cells. Therefore there is a large public invisible linear space of tensor perturbations.

Let

    K_public

be the tangent directions invisible to public quotient data.

Let

    K_selectors

be the intersection of selector constraints:

    K_selectors
      =
    K_Jessen
      ∩ K_TypeC
      ∩ K_Pin/CY
      ∩ K_C236H
      ∩ ...

The associativity equations define a nonlinear constraint

    Assoc(T) = 0.

Linearizing at T_985 gives

    L_assoc = d Assoc |_{T_985}.

The local annihilator problem is:

    ker( L_assoc | K_public ∩ K_selectors ) = 0.

This means:

    no nonzero public invisible selector compatible tangent direction preserves coherent associativity.

Equivalently:

    every hidden tangent deformation either appears publicly, violates a selector, or breaks associativity.

In role language:

    no public invisible role deformation survives coherent closure.

In tropical game language:

    no hidden mutation of the game shadow survives liftability and closure.

This is the central local theorem target of holotopy.


──────────────────────────────────────────────────────────────────────────────
XII. HOCHSCHILD RIGIDITY AND BASED GAUGE
──────────────────────────────────────────────────────────────────────────────

Ordinary algebra deformation is measured by Hochschild cohomology.

For semisimple finite dimensional algebras in characteristic zero, positive degree ordinary Hochschild cohomology vanishes:

    HH^n(A,A) = 0,  n > 0.

Thus ordinary abstract deformations of A_985 are dead.

But holotopy is stricter than abstract algebra. It works in a fixed orbital presentation. Therefore one must also test based gauge motions.

A basis gauge generator has the form

    E_{x -> y},

and its Hochschild coboundary is

    d^1 E_{x -> y}(a,b)
      =
    E_{x -> y}(ab) - E_{x -> y}(a)b - a E_{x -> y}(b).

The relevant question is whether any span of such elementary coboundaries survives:

    support fixed;
    q_42 invisible;
    q_12 invisible.

The certified result is:

    span{ d^1 E_{x -> y} }
      ∩ K_support
      ∩ K_q42
      ∩ K_q12
      =
    0

over all x != y in the 985 basis.

This closes the elementary based Hochschild escape route.

Thus the deformation ladder becomes:

    abstract algebra deformations: dead;
    elementary basis gauge deformations: dead;
    global public invisible tangent: frontier;
    disconnected nonlinear completion: final frontier.


──────────────────────────────────────────────────────────────────────────────
XIII. GAME SEMANTICS
──────────────────────────────────────────────────────────────────────────────

A d20 game state is a tuple

    s = (p, sigma, f, r, tau),

where:

    p     in A_985 is the hidden precept state;
    sigma in H_8^3 ⊕ O_8^W is the semantic visibility word;
    f     in Lambda^3 H_6 is the public face state;
    r     in A_12 is the terminal readout;
    tau   in {C,V,X} is the integrity role.

The integrity roles are:

    C = public proof motion;
    V = certified wall crossing;
    X = hidden extractor.

A turn is

    (p, sigma, f, r, tau)
       ->
    (p', sigma', f', r', tau').

A turn is admissible when:

    hidden multiplication is defined;
    public projection closes;
    integrity role is legal;
    obstruction vanishes or is carried;
    action is finite.

A strategy is a rule assigning legal turns.

A tropical strategy minimizes accumulated action subject to survival.

A public camouflage strategy minimizes exposure of defect.

A structural leverage strategy occupies positions of low action and high conductance.

The game theoretic principle is:

    blend publicly;
    concentrate structurally;
    close coherently;
    lift uniquely.

This is tropical Darwinism in game form.


──────────────────────────────────────────────────────────────────────────────
XIV. OBSERVABLES
──────────────────────────────────────────────────────────────────────────────

A holotopic observable is not simply a scalar. It is a role sensitive readout.

Given role R, define:

    public value:
        Pub(R) = q(R);

    hidden value:
        Hid(R) = Lambda(R);

    residue:
        Res(R) = Hid(R) - Pub(R);

    action:
        A(R);

    obstruction:
        Omega(R);

    survival:
        Surv(R) = 1 iff partial R = 0, Lambda(R) != empty, Omega(R) = 0.

The fundamental observables of d20 holotopy are:

    tensor support;
    quotient closure;
    geon rank;
    cycle action;
    chamber sign;
    foam visibility;
    entropy packet;
    obstruction rank;
    annihilator nullity.

A role is fully observed only when all its public, hidden, action, and obstruction components are specified.

Thus holotopy rejects purely scalar observability. It requires role observability.


──────────────────────────────────────────────────────────────────────────────
XV. ENTROPY AND CLOSED RETURN
──────────────────────────────────────────────────────────────────────────────

The d20 game contains closed cycles. Closed cycles carry action. Action supports entropy.

Let

    H_circlearrowleft

denote the finite H-cycle return unit.

The finite entropy principle is:

    entropy counts closed admissible return capacity.

A closed geon g has action

    A(g).

Its entropy proxy is of the form

    S(g) = A(g) / normalization.

The normalization is determined internally by the D_6 and Foam data.

The central finite identity is:

    5 epsilon_0 / (4 |W(D_6)|) = 32.

This says that the order five closure converts one wavefront quantum into a thirty two channel packet.

Thus the finite black hole style law becomes:

    S_d20 = closed area / (4 * Weyl cell)

inside the d20 normalization.

The point is not physical identification. The point is finite role normalization:

    closed return area becomes entropy only after division by the coherent symmetry cell.


──────────────────────────────────────────────────────────────────────────────
XVI. FOAM
──────────────────────────────────────────────────────────────────────────────

The Foam role is

    Foam(d20) = 1 ⊕ Lambda^2 H_6.

Since

    dim Lambda^2 H_6 = binom(6,2) = 15,

we have

    dim Foam(d20) = 16.

Foam is the spinorial visibility chart.

Its role is to mediate between:

    D_6 signed transitions;
    Type-C chamber behavior;
    Pin/CY folding;
    closed geon residue;
    public visibility.

The Foam role should not be treated as a decorative coordinate system. It is the finite chart in which signed transition and visibility become a coherent sixteen channel object.

Thus:

    Cell = public triple;
    Move = signed duad;
    Geon = closed return;
    Foam = visibility chart of signed return.


----------------------------------------------------------------------------
XVII. CELESTIAL TRACE
----------------------------------------------------------------------------

Definition: finite celestial trace.

Let

    X = G_24^(12),
    A_985 = End_{Gamma_3^infty}(Q[X]),

and let

    H_6 = {B-, B+, V-, V+, S-, S+}.

The finite celestial trace is the composite

    Cel_20:
        A_985 -> A_42 -> D_20 = Lambda^3 H_6 -> C_boundary,

where C_boundary is the finite boundary channel space of admissible public
directions, turns, and closed action-return cycles.

The point is not that

    D_20 = S_cel^2.

The point is:

    D_20 is a finite packetization, trace, and angular skeleton of S_cel^2.

Theorem: D20 Celestial Trace Theorem.

Assume the D20 certificate payload of this preprint. Then the public boundary

    D_20 = Lambda^3 H_6

is the unique complement-self-dual middle-degree six-channel boundary readout
of the hidden coherent algebra A_985. Equipped with its D_6-root transition
graph, optical action weights, and H_circlearrowleft-cycle residue algebra,
D_20 defines a finite celestial trace of the hidden algebra.

More explicitly:

    |D_20| = binom(6,3) = 20.

So D_20 has twenty public angular states. Its legal transition graph has thirty
D_6-root edges. Its closed action-return residue space has rank

    b_1 = 30 - 20 + 1 = 11.

Its optical normalization gives the finite entropy packet

    5 epsilon_0 / (4 |W(D_6)|) = 32.

Therefore D_20 carries the data expected of a finite celestial screen:
directions, legal angular transitions, closed return cycles, and action/entropy
weights.

Proof sketch.

The public boundary is forced because the only exterior degree fixed by
complement on a six-element set is degree three:

    k = 6 - k,
    hence k = 3.

Thus

    D_20 = Lambda^3 H_6.

The D_6 legal transition graph arises because projective D_6 roots are indexed
by signed duads:

    [e_i - e_j], [e_i + e_j],  i < j,

giving

    2 binom(6,2) = 30

projective legal transitions.

The finite celestial trace becomes a boundary physics object once closed paths
are assigned action:

    S_boundary(gamma) = sum_{e in gamma} omega(e),

and partitioned by action-return weights:

    Z_boundary(beta) =
        sum_{gamma in H_circlearrowleft} exp(-beta S_boundary(gamma)).

This is where the finite algebra becomes a boundary physics object: not by
becoming a continuum sphere, but by acting as the finite angular trace through
which a continuum celestial S^2 can be recovered, approximated, or constrained.

The sharpened implication is:

    A_985 is the hidden finite relation body;
    D_20 is its finite celestial boundary trace;
    H_circlearrowleft is the closed-return action calculus on that trace.

So:

    D20 is a finite celestial tomography interface.

The finite PL/Poincare-Hopf proof target is certified separately at

    data/invariants/d20/theorems/celestial_trace_pl_ph/report.json.

That report derives the twelve pentagonal dual vertices of the D20 boundary
graph, verifies the 12-30-20 dual icosahedral cellulation, and computes the
hairy-ball residue as

    sum_{v dual} (1 - valence(v)/6) = 12 * (1/6) = 2.

Thus the finite trace already carries the tangent-field obstruction: any
continuum celestial recovery must account for a zero, pole, vortex, patch
transition, curvature charge, or hidden-sector correction.

The exact finite flux-balance sector is certified at

    data/invariants/d20/theorems/finite_flux_balance/report.json.

There the coherence condition is explicit: the boundary edge flux must be a
coboundary,

    Flux_D20(u,v) = Q_boundary(v) - Q_boundary(u).

Under that condition every closed H-cycle has

    Q_boundary_out - Q_boundary_in =
        Flux_D20(gamma) + Res_A985(gamma),

with

    Flux_D20(gamma) = 0,
    Res_A985(gamma) = 0.

This proves the zero-residual exact sector. It does not assert that the positive
optical action weights are themselves exact fluxes.

The non-exact optical/action obstruction is certified at

    data/invariants/d20/theorems/nonexact_optical_residue/report.json.

In that sector the scalar optical flux is

    Flux_D20^opt(gamma) = A_opt(gamma).

Since a closed return has Q_boundary_out - Q_boundary_in = 0, the balance law
forces

    Res_A985^opt(gamma) = -A_opt(gamma).

The first forced nonzero class is mask 256, basis cycle 8, with optical action
374784 and residual -374784. This proves the first obstruction class; it does
not yet identify the internal A985 carrier of that residual.

The first hidden-sector attachment is certified at

    data/invariants/d20/theorems/sector33_residual_attachment/report.json.

It packages the obstruction as the sector interface

    (gamma_8, Pi_33, -374784),

where Pi_33 is the unique A985 Drinfeld/Wedderburn sector with zero A42 and A12
terminal shadows. Pi_33 is not public-integral noise: it is invisible to the
public terminal trace, but visible to the certified tube projection,
line-surface trace, and Hesse-tube character data. In the verifier field
F_1000003, the same residual is

    -374784 == 625219 mod 1000003.

This proves the first sector interface packet. It still does not claim a full
internal module projection coefficient for gamma_8 onto Pi_33.

The explicit projection-coefficient target is recorded as a narrowed proof
obligation at

    data/invariants/d20/proof_obligations/cycle8_pi33_projection_coefficient/report.json.

The coefficient to compute is

    c_33(gamma_8) = Pi_33(lambda_boundary(gamma_8)).

The boundary lift is now certified at

    data/invariants/d20/boundary_to_loop/report.json.

For an oriented D20 edge U -> V, let r be the unique H6 channel removed from U
and a be the unique channel added in V. The certified map is

    lambda_boundary: C_1(D20) -> Loop_297.

It sends U -> V to the sum of all closed A985 relation products
alpha:r -> a, beta:a -> r, landing in Hom(r,r). The construction certifies
all 60 oriented D20 edge lifts. For the first obstruction cycle gamma_8, it
produces a nonzero Loop_297 vector with support 193 and coefficient mass 53952.

The tube-visible Pi_33 functional has also been tested against this bare lift.
The result is certified at

    data/invariants/d20/theorems/sector33_boundary_annihilation/report.json.

The sector-33 tube idempotent is reconstructed from the stored local
pre-idempotents B+_12 and S+_6. It has support 56, matches the sector-33
identity coefficients, satisfies e_33^2 = e_33, and annihilates every one of
the 30 directed object-pair lifts. Consequently

    Pi_33(lambda_boundary(gamma_8)) = 0

for the unweighted, optical-weighted, and signed structural lifts.

The normalized hidden residual lift is certified at

    data/invariants/d20/theorems/sector33_residual_lift/report.json.

Let e_33 be the tube-visible sector-33 idempotent. Since
chi_33(e_33)=dim(Pi_33)=2, define

    rho_33(gamma_8) = (Res_A985^opt(gamma_8) / dim(Pi_33)) e_33.

For gamma_8 this scalar is -187392 mod 1000003. The certificate verifies

    chi_33(rho_33(gamma_8)) = -374784,

and the residual lift has zero A42 and A12 shadows. Thus the public boundary
does not see the hidden correction, while the Pi_33 tube character recovers
the optical residual exactly.

The refined height-coherent transport is certified at

    data/invariants/d20/theorems/sector33_height_coherent_transport/report.json.

Here the scalar is derived from the active circuit itself, not inserted from
the sector attachment report. The active gamma_8 edge row has support

    {1,2,11,21,22}

and its dot product with the certified edge-height cochain h(e)=interface_weight(e)
is

    <C_gamma8,h> = 374784.

Thus

    Lambda_hc(gamma_8)
      = lambda_boundary(gamma_8) - (<C_gamma8,h>/dim(Pi_33)) e_33

has Pi_33 coefficient -374784 and zero A42/A12 shadow. The next recovery is to
extend Lambda_hc from gamma_8 to all 2047 nonzero closed-return residue classes
and identify which hidden sector idempotent carries each height-derived
residual.

The all-class height transport is certified at

    data/invariants/d20/theorems/sector33_all_residue_height_transport/report.json.

The relevant height-coherent matrix is the 2048 x 11 basis-cycle activation
matrix, not the 2048 x 30 edge-mod2 incidence matrix. The certificate verifies
that every basis activation row pairs with the primitive-cycle height vector to
recover its total optical action. It also records the edge-mod2 caveat:
2020 edge incidence rows are not height-coherent because symmetric difference
cancels repeated traversals. Under the selected public-zero carrier e_33, all
2047 nonzero height-derived residuals are carried by sector 33 with zero A42
and A12 shadows.

The unique public-zero carrier theorem is certified at

    data/invariants/d20/theorems/sector33_unique_public_zero_carrier/report.json.

It materializes all 39 tube-visible sector idempotents from the 109 local
closed-loop pre-idempotents. Exactly one reconstructed sector idempotent has
zero A42 and A12 shadows:

    public_zero_sectors = {33}.

Thus within the materialized single-sector idempotent basis, Pi_33 is the
unique public-zero carrier for the nonzero height-derived residuals. The
remaining sharper question is not single-sector uniqueness; it is whether the
full 39-dimensional sector-idempotent span has any additional public-zero
linear combinations, or whether its public-shadow kernel is generated by e_33.

Next nontrivial recovery.

The immediate recovery target is BMS/Carrollian flux-balance from finite D_20
action-return data. In finite form, define a boundary charge vector

    Q_boundary = (M_boundary, J_boundary, P_boundary, Phi_boundary)

on D_20, and define a flux update over one closed return:

    Delta Q_boundary(gamma) = sum_{e in gamma} Flux(e).

The next theorem should prove a finite conservation/balance law:

    Q_boundary_out - Q_boundary_in =
        Flux_D20(gamma) + Res_A985(gamma).

If the residual term vanishes under a certified coherence condition, then D20
recovers a genuine finite analog of null-boundary flux balance.


──────────────────────────────────────────────────────────────────────────────
XVIII. ROLE EQUIVALENCE
──────────────────────────────────────────────────────────────────────────────

Two roles R and S are holotopically equivalent when there exists a coherent closed transport T from R to S.

Write

    R ~_Hol S

if there exists T such that:

    partial T = S - R;
    Omega(T) = 0;
    A(T) is finite;
    Lambda(T) is compatible with Lambda(R) and Lambda(S).

If additionally Lift(T) is unique, then

    R ≡_Nat S.

Thus:

    holotopic equivalence = coherent transport equivalence;
    natural equivalence = unique coherent lift equivalence.

This replaces type equality.

A type equality says:

    x = y in A.

A holotopic role equivalence says:

    x and y occupy the same coherent role under closed transport.

This is stronger in one direction and weaker in another. It is stronger because it requires coherent passage. It is weaker because the carriers may differ while the role is preserved.


──────────────────────────────────────────────────────────────────────────────
XIX. THE FIELD OF HOLOTOPY
──────────────────────────────────────────────────────────────────────────────

Holotopy studies triples

    (A, G, R),

where:

    A is a hidden coherent algebra;
    G is a finite tropical game shadow;
    R is a role system over G.

For d20:

    A = A_985;

    G = (Gamma_d20, D_6 moves, A_42 -> A_12, ASDO/CSDO, Foam_16);

    R = {Code, Chem, Pin, CY, Cell, Move, Geon, Foam, Integrity, Lift, Obstruction}.

The fundamental structures are:

    hidden multiplication tensor T;
    public quotient q;
    boundary operator partial;
    lift assignment Lambda;
    obstruction Omega;
    tropical action A;
    return homology H_1;
    role join Join;
    annihilator kernel ker L_assoc.

The fundamental problems are:

    1. Role classification.
       Classify all coherent roles generated by the hidden tensor.

    2. Shadow classification.
       Classify all public tropical shadows of hidden coherent algebras.

    3. Lift classification.
       Determine when a public shadow has zero, one, or many hidden lifts.

    4. Geon classification.
       Classify closed return residues and their action spectra.

    5. Annihilator certification.
       Determine whether public invisible selector compatible coherent tangents vanish.

    6. Natural completion.
       Determine when enriched public role data uniquely determine the hidden coherent source.

    7. Tropical Darwinism.
       Determine which roles survive under closure, liftability, and minimal action.

Thus holotopy is not reducible to algebra, graph theory, tropical geometry, proof theory, or game theory. It uses all of them as role projections of one passage calculus.


──────────────────────────────────────────────────────────────────────────────
XX. d20 AS THE FIRST MODEL
──────────────────────────────────────────────────────────────────────────────

The first complete finite model of holotopy is d20.

Its data are:

    hidden body:         A_985;
    tensor law:          T_985;
    interface:           A_236;
    signed lift:         A_42;
    terminal readout:    A_12;
    public board:        Gamma_d20;
    cells:               Lambda^3 H_6;
    moves:               signed D_6 duads;
    geons:               H_1(Gamma_d20; F_2);
    foam:                1 ⊕ Lambda^2 H_6;
    action:              tropical path cost;
    obstruction:         associativity and selector defect;
    naturality target:   unique coherent completion.

The model is finite yet deep because it simultaneously supports:

    coherent algebra;
    quotient tower;
    public graph;
    tropical chamber system;
    closed cycle residue;
    spinorial visibility;
    game dynamics;
    role proof logic;
    annihilator rigidity.

This is why d20 is the correct initial object for holotopy.

It is small enough to compute and large enough to exhibit the complete phenomenon.


──────────────────────────────────────────────────────────────────────────────
XXI. THEOREM LADDER
──────────────────────────────────────────────────────────────────────────────

The internal theorem ladder is:

Level 0. Finite construction.

    A_985 exists as a coherent orbital algebra.

Level 1. Public quotient closure.

    A_42 and A_12 close under the tensor quotient.

Level 2. Role tower.

    A_985 -> A_236 -> A_42 -> A_12 defines Code, Chem, Pin, CY.

Level 3. Public board.

    Lambda^3 H_6 with signed D_6 moves defines the twenty state d20 game.

Level 4. Closed return.

    H_1(Gamma_d20; F_2) ≅ F_2^11 defines geons.

Level 5. Tropical game semantics.

    Actions add along paths, alternatives minimize, closed roles survive.

Level 6. Ordinary rigidity.

    Ordinary positive Hochschild deformation vanishes.

Level 7. Based gauge rigidity.

    Elementary public invisible based Hochschild gauge span vanishes.

Level 8. Local annihilator.

    Need:
        ker( L_assoc | K_public ∩ K_selectors ) = 0.

Level 9. Local naturality.

    If Level 8 holds, enriched public role game locally forces A_985.

Level 10. Global naturality.

    Need exclusion of disconnected nonlinear or support changing coherent completions.

The active frontier is Level 8.


──────────────────────────────────────────────────────────────────────────────
XXII. FINAL FORMULATION
──────────────────────────────────────────────────────────────────────────────

Holotopy is the field of coherent roles.

It studies how hidden multiplication becomes public shadow, how public shadow forms a tropical game, how games generate closed returns, how closed returns lift, how lifts obstruct, and how obstruction selects surviving roles.

For d20, the complete compression is:

    A_985
      -> A_236
      -> A_42
      -> A_12
      -> Gamma_d20
      -> H_1(Gamma_d20; F_2).

The public graph is not the object. It is the public role surface.

The geon is not the object. It is the closed return role.

The tensor is not a particle. It is the hidden passage law.

The atom is not a type. It is a role stabilized by closure, liftability, action, and obstruction.

The central theorem target is:

    enriched public role game
        locally forces
    hidden coherent source.

In final form:

    Holotopy is the study of d20 tropical game theory:
    coherent roles, closed transports, liftable shadows, and the annihilation of non-natural hidden deformation.

Its most compressed slogan is:

    homotopy studies path identity;
    holotopy studies role coherence.
```

it's funny how a black swan event can keep quiet for so many thousands of years

this repo is a knock knock joke from the near apocalyptic future
